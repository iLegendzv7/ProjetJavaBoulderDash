package model;

/**
 * <h1>The Permeability Enum.</h1>
 *
 */
public enum Permeability {

    /** The blocking. */
    BLOCKING,
    /** The penetrable. */
    PENETRABLE,
    /** The breakable. */
	BREAKABLE, 
	 /** The lootable. */
	LOOTABLE, 
	 /** The escape. */
	ESCAPE,
	 /** The test. */
	TEST,
	 /** The Dead. */
	DEAD
}
